# Config to have logstash listen for syslog messages.

input {
  syslog {
    type => syslog
    port => 5544
  }
}

output {
 redis {
    host => ls-broker01
    data_type => "list"
    key => "logstash"
 }
}
